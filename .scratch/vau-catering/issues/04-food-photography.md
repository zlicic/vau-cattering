# 04 — Real food photography on Cloudinary

**What to build:** Every menu item displays an actual food photograph instead of a Cloudinary demo placeholder. Images are optimized and load fast.

**Blocked by:** None — can start immediately (parallel with 03).

**Status:** ready-for-agent

- [ ] Food photos uploaded to Cloudinary for every menu item
- [ ] `src/data/menu.json` image URLs updated to real Cloudinary URLs
- [ ] Cloudinary transforms applied: `f_auto,q_auto,w_600` for grid thumbnails, `w_800` for modal view
- [ ] All images have descriptive `alt` text in Serbian
